#include <iostream>
using namespace std;
int main()
{
	cout << "chuong trinh tinh tong hai so" << endl;
	int so1, so2; // khai bao bien so1 va so2 kieu so nguyen
	int tong;
	so1 = 3; // gan bien so1 va gia tri so3
	so2 = 9;
	tong = so1 + so2;
	cout << "tong hai so:" << so1 << "+" << so2 << "=" << tong;
	return 0;
}